export const metadata = {
  title: 'Udyam – Step 1 & 2 (Frontend)',
  description: 'Frontend-only mock for Aadhaar & PAN verification steps',
};

import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
